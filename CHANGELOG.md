# PeekShield 窥屿盾

# 更新日志（Changelog）

## 1.2.1.3

* 安全设置新增「进程保护」：

  * 开启后若软件进程被强制结束（如任务管理器结束任务、进程崩溃），将自动重启，防止他人通过关闭软件绕过防偷窥保护。
  * **被杀即锁屏（防篡改）**：守护进程检测到主程序在的情况下消失（被强杀或崩溃），会立即锁定系统会话。对偷窥者而言，关掉软件即触发锁屏，攻击直接失败。
  * 需先设置密码方可启用；关闭密码保护时会同步关闭进程保护。
  * 修复进程保护看门狗存活判定逻辑（原判定恒为假，主程序被强制结束后守护进程不触发重启/锁屏；改为检测「存活→死亡」跳变、连续 2 次确认死亡即锁屏并自动重启）。
  * 移除调试/测试残留：模拟软件崩溃、模拟被强制结束、摄像头测试打开、崩溃测试卡片、DIAG 诊断日志、帧诊断日志与调试帧保存。

## 1.2.1.2

* 安全设置新增「快捷验证（自动刷脸解锁）」（可选）：

  * 在需要验证的场景（解锁主页面 / 安全设置 / 退出 / 卸载）会先自动调用摄像头识别机主，识别成功即自动解锁，无需输入密码、也无需手动点击。
  * 复用已录入的机主人脸（防偷窥录入），无需再单独录入解锁人脸；但需先设置密码，且已录入机主人脸方可启用。
  * 未识别到机主时 8 秒自动结束，回退到密码验证。

## 1.2.1.0

* 安全设置新增「刷脸解锁」：

  * 复用同一本地 dlib 人脸识别模型，可用刷脸等效代替密码完成验证（解锁主页面 / 安全设置 / 退出 / 卸载）。
  * 设置人脸前必须先设置密码；密码与刷脸可同时使用，也可单独使用密码。
  * 刷脸解锁特征独立存储于本机 `enrollment/unlock/`，与防偷窥人脸录入互不干扰，可随时重新录入或关闭。
  * 录入支持两种方式：本机摄像头实时采集，或直接上传一张正脸照片完成录入。
  * 未识别到正确人脸时 8 秒自动结束，可改用密码验证。

## 1.2.0.2

* 新增密码验证失败锁定机制：

  * 连续输错达到阈值（5 次）后锁定账号，锁定期间不可重试。
  * 阶梯等待时间：首次锁定 1 分钟，其后依次为 5 / 10 / 30 / 60 / 120 / 240 分钟，连续被锁按更长时间递增（封顶 4 小时）。
  * 验证成功后重置失败计数与锁定状态。

## 1.2.0.1

* 修复密码保护时效性问题：

  * 关闭主页面或安全设置页面后自动重新锁定，再次打开需重新验证密码。
  * 密码验证后新增时效性：默认 15 分钟内有效，超时自动重新锁定。
  * 安全设置内新增「验证后保持解锁（分钟）」可调项，可自定义解锁保持时长。
* 修复后台计时器未在会话失效时及时重锁定界面的问题。

## 1.2.0.0

* 新增「安全设置」：

  * 可设置自定义密码，密码以哈希形式保存。
  * 可自选是否设置保密问题，用于忘记密码时找回。
  * 可自定义密码保护范围：退出应用、卸载应用、打开主页面、打开安全设置菜单，均可单独开关。
  * 安全设置内可修改密码、关闭密码保护（需先验证）。
* 新增「忘记密码」自助找回。

## 1.1.0.0

* 新增「软件更新」功能：

  * 启动时自动检测应用版本是否为最新（可在设置中关闭）。
  * 检测到新版本时，展示最新版本号与更新日志，并询问是否在应用内更新。
  * 支持「应用内更新」：自动下载对应平台安装包并覆盖安装。
  * 新增手动更新入口：可手动「检查更新」，并选择发布源前往下载（GitHub Releases / 123 网盘）。
  * 新增设置项：「启动时自动检查更新」「有更新时自动静默更新」，可分别开关。
* 发布源：GitHub Releases、123 网盘（详见README.md）。

## 1.0.0.0

* 首个正式版本。
* 本地离线隐私防窥：AI 人脸识别、偷窥检测、雾化遮罩、告警、受保护程序列表。
* GUI 设置、系统托盘、开机自启动、人脸录入（摄像头 / 照片）、隐私同意门控。
* 跨平台支持：Windows / macOS / Linux 三端发布。

